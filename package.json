{
  "name": "proyecto-votaciones-root",
  "version": "1.0.0",
  "private": true,
  "description": "Helper package.json at repo root to trigger server install on Render",
  "scripts": {
    "postinstall": "cd server && npm install",
    "start": "cd server && node index.js"
  },
  "engines": {
    "node": ">=18"
  }
}
